services:
  - type: web
    name: crash-monitor
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python crash_monitor.py
    envVars:
      - key: GAME_URL
        value: https://gecwlsp6ylfo.pro/en/games/crash  # Replace with your actual game URL
